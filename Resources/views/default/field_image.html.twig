<a href="#" class="harmonyadmin-thumbnail" data-featherlight="#harmonyadmin-lightbox-{{ uuid }}" data-featherlight-close-on-click="anywhere">
    <img src="{{ asset(value) }}">
</a>

<div id="harmonyadmin-lightbox-{{ uuid }}" class="harmonyadmin-lightbox">
    <img src="{{ asset(value) }}">
</div>
